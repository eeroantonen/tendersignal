# Optional. Leave unset for recruiter demo mode; the app will use bundled real public cache data.
# Add this only in Streamlit Cloud secrets if you want live Hilma refresh from the hosted app.
HILMA_AVP_SUBSCRIPTION_KEY = ""
